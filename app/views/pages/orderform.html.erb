<%= render 'shared/navbar_general' %>

<div class="form">
  <%= simple_form_for @customer do |f| %>
    <%= f.input :email %>
    <%= f.input :postcode, label: 'Enter the first part of your postcode' %>
    <%= f.button :submit, 'Create Order', class: 'view-plan-btn' %>
  <% end %>
</div>
