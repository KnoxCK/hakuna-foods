<div class="form">
  <%= form_tag customer_customer_plan_extra_items_path(@customer.id), method: :post %>
  <table>
    <thead>
      <tr>
        <th>Item</th>
        <th>Price</th>
        <th>Quantity per week</th>
      </tr>
    </thead>
    <tbody>
    <% @extra_items.each do |product| %>
      <tr>
        <td><h4><%= product.name %></h4></td>
        <td><p><%= product.daily_price.to_f %></p></td>
        <td><%= number_field_tag "#{product.name}", nil %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
  <%= submit_tag 'Submit' %>
</div>
